# Offline Invoice — Python dependencies
# Install with: py -3.11 -m pip install -r requirements.txt

# ── UI framework ──────────────────────────────────────────────────────────────
PySide6>=6.7.0               # replaces customtkinter

# ── PDF generation ────────────────────────────────────────────────────────────
weasyprint==57.2
pydyf==0.5.0
cffi>=1.17.0
cairocffi>=1.7.1

# ── Image handling ────────────────────────────────────────────────────────────
Pillow>=10.4.0               # logo image handling + preview decode
pdf2image>=1.17.0            # live preview (requires Poppler on PATH)
qrcode[pil]>=7.4.2           # QR code generation for payment links

# ── Templates ─────────────────────────────────────────────────────────────────
Jinja2>=3.1.4

# ── Build ─────────────────────────────────────────────────────────────────────
pyinstaller>=6.11.0          # package to .exe

